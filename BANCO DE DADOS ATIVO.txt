BANCO DE DADOS ATIVO
-> Reagir Automaticamente
* Gatilhos (Triggers)
* Restrições (Constrain)
* Procedimentos (Stored Procedure)

batch = bloco de comando a ser executado

DECLARE @nome VARCHAR(50);
SET @nome = 'Juca';
      ou
SELECT @nome = nome
FROM aluno
Where id = "S";

DECLARE está em {memória}
